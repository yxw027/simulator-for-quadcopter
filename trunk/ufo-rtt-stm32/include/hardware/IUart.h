/** * @file IUart.h * UART interface */#ifndef _IUART_H#define _IUART_H/** * @addtogroup ufo * @} *//** * @addtogroup Hardware Abstract Layer * @{ *//**  * packet type definitions  */enum packet_type {    GET_GYRO_DATA,    GET_ACCL_DATA,        MAX_PACKET_TYPE};#define MAX_PACKET_LEN  28typedef struct uart_packet_t {    uint8_t type;                       /**< packet type */    uint8_t length;                     /**< packet length */    uint8_t buf[MAX_PACKET_LEN];        /**< buffer of the packet */} uart_packet_t;/** * send a packet to via uart * * @param packet the packet sent to uart * @return success 0, negetive otherwise */int uart_packet_tx(struct uart_packet_t *packet);/** * send a gyro packet via uart * * @return 0 success, negetive otherwise */int gyro_packet_tx();/** @} *//** @} */#endif /* _IUART_H */